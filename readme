Date : 29 November 2016

This documents tell which files are used to obtain what output.


Files at the beginning : 

mouse_qualitative.txt : Contains all information on Mouse. Rest of files inherits crucial Mouse's data from this File.
mouse_human.fasta.txt : Transloop output for the mouse
mouse1_loopstat_input2 : Second input to the loopstat (<ipi><length><topology><FD><LD><FD.l><LD.l><Avg.FD><Avg.LD>)

Steps : 

1. Prints only identifier and peptide sequences for each entry in mouse_qualitative.txt 
    >> awk 'BEGIN{FS = "\t"}{print $1"\t"$4}' mouse_qualitative.txt > mouse-v1_seq.txt
    
    mouse_qualitative ----> mouse-v1_seq.txt(n_glyco file)    (Lines = 1233)

2. Now we obtains the peptide file.
    
    mouse.fasta(--fasta) + mouse_seq2hash.txt(--human) ——> find_glyco_pep.cpp ——> peptide output (<identifier> <peptide_sequence>)
    mouse-v1_seq.txt ------> find_glyco_pep.cpp ----------------> peptide-v1.txt (822)
    
3. To compile the first loopstat input, Mapping of different criterias such as description,protein length is initiated for each identifier in peptide file.

    Run prepare_worm_loopstat_output1.py --------> worm_loopstat_input1_exp.txt (t1[current]-v1)


4. Now worm_loopstat_input1_exp.txt + worm_loopstat_input1_theo.txt ---------> loopstat.cpp --------------> mouse_loopstat_output1_exp.txt + mouse_loopstat_output2_exp.txt

